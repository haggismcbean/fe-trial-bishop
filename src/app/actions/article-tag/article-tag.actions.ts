import { createAction } from '@ngrx/store';

export const create = createAction('[Article Tag] Create');
export const add = createAction('[Article Tag] Add');
export const remove = createAction('[Article Tag] Remove');
